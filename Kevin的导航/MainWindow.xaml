<Window x:Class="Kevin的导航.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kevin的导航"
        mc:Ignorable="d"
        xmlns:views="clr-namespace:Kevin的导航.Views"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        Title="MainWindow" Height="450" Width="800">
    <TabControl ItemsSource="{Binding ViewModels}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Header}"/>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.Resources>
            <DataTemplate DataType="{x:Type local:Page1ViewModel}">
                <views:Page1/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type local:Page2ViewModel}">
                <views:Page2/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type local:Page3ViewModel}">
                <views:Page3/>
            </DataTemplate>
        </TabControl.Resources>
    </TabControl>
</Window>
