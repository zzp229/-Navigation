<Window x:Class="区域导航.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:区域导航.Converters"
        xmlns:local="clr-namespace:区域导航"
        xmlns:vm="clr-namespace:区域导航.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel></vm:MainViewModel>
    </Window.DataContext>

    <Window.Resources>
        <conv:ApplicationPageViewConverter x:Key="conv"/>
    </Window.Resources>
    <ContentControl Content="{Binding CurrentPage, Converter={StaticResource conv}}"/>
</Window>
